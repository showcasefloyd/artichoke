import React from 'react';
import ReactDOM from 'react-dom/client';
import '../../../sass/main.scss';

const rootEl = document.getElementById('root');
if (rootEl) {
    ReactDOM.createRoot(rootEl).render(<div>Admin loading&hellip;</div>);
}
